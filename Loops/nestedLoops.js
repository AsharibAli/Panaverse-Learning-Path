const products = ["Shirt", "Trouser", "Jacket"];
const gifts = ["Pen", "Keychain", "Tie"];

for (let i = 0; i < products.length; i++) {
  //   console.log(products[i]);
  for (let a = 0; a < gifts.length; a++) {
    console.log(products[i] + " - " + gifts[a]);
  }
}
